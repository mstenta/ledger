<?php

/**
 * @file
 * Ledger.
 */

/**
 * Implements hook_entity_info().
 */
function ledger_entity_info() {
  return array(
    'account' => array(
      'label' => t('Account'),
      'base table' => 'ledger_account',
      'entity keys' => array(
        'id' => 'aid',
      ),
      'fieldable' => TRUE,
    ),
    'transaction' => array(
      'label' => t('Transaction'),
      'base table' => 'ledger_transaction',
      'entity keys' => array(
        'id' => 'tid',
      ),
      'fieldable' => TRUE,
    ),
  );
}

/**
 * Implementation of hook_perm().
 */
function ledger_permission() {
  return array(
    'access ledger' => array(
      'title' => t('Access Ledger'), 
      'description' => t('Access the ledger interface.'),
    ),
    'administer ledger' => array(
      'title' => t('Administer Ledger'), 
      'description' => t('Access the ledger interface.'),
    ),
  );
}

/**
 * Implementation of hook_menu().
 */
function ledger_menu() {
  $items['ledger'] = array(
    'title' => 'Ledger', 
    'page callback' => 'ledger_page', 
    'access arguments' => array('access ledger'),
  );
  return $items;
}

/**
 * Ledger page callback
 */
function ledger_page() {
  $output = 'ledger';
  return $output;
}